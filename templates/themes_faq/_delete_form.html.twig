<form method="post" action="{{ path('themes_faq_delete', {'id': themes_faq.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ themes_faq.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
