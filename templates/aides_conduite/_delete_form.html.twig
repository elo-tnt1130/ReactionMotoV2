<form method="post" action="{{ path('aides_conduite_delete', {'id': aides_conduite.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aides_conduite.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
