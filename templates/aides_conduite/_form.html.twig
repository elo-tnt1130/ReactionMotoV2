{{ form_start(form) }}
{{form_label (form.lib_aide_conduite)}} <font style="color:red">*</font>
{{form_widget (form.lib_aide_conduite)}}
<button class="btn btn-info mt-3">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
