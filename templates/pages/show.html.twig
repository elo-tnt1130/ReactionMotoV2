{% extends 'base.html.twig' %}

{% block title %}Pages
{% endblock %}

{% block body %}
	<main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-md-4 p-4">
		<div class="col-12">
			<h1>Page -
				{{ page.libPage }}
			</h1>
			<a href="{{ path('pages_index') }}" class="btn btn-info">
				<i class="fa fa-arrow-left"></i>
				Retour à la liste
			</a>
		</div>
		<div class="container my-3">
			<table class="table">
				<tbody>
					<tr>
						<th>Titre de la page</th>
						<td>{{ page.libPage }}</td>
					</tr>
					<tr>
						<th>Bloc de texte 1</th>
						<td>{{ page.bloc1|raw }}</td>
					</tr>
					<tr>
						<th>Bloc de texte 2</th>
						<td>{{ page.bloc2|raw }}</td>
					</tr>
					<tr>
						<th>Bloc de texte 3</th>
						<td>{{ page.bloc3|raw }}</td>
					</tr>
					<tr>
						<th>Bloc de texte 4</th>
						<td>{{ page.bloc4|raw }}</td>
					</tr>
					<tr>
					<tr>
						<th>Description</th>
						<td>{{ page.metadescr }}</td>
					</tr>
					<tr>
						<th>Mots clefs</th>
						<td>{{ page.metakw }}</td>
					</tr>
				</tbody>
			</tr>
		</tbody>
	</table>
</div>
<div class="container">
	<div class="row">
		<a href="{{ path('pages_edit', {'id': page.id})}}" class="btn btn-info mr-3">
			<i class="fa fa-pen"></i>
			Modifier</a>

		{{ include('pages/_delete_form.html.twig') }}
	</div>
</div></main>{% endblock %}
